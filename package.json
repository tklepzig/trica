{
  "name": "nisaba",
  "version": "0.1.0",
  "type": "module",
  "description": "Offline trigonometry triangle solver. Named after Nisaba, the Sumerian goddess of writing, accounting, and measurement.",
  "scripts": {
    "start": "npx --yes live-server .",
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json --noEmit && npx --yes jest",
    "dev": "tsc -w"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.7.0",
    "ada-ui": "^6.0.1",
    "sass": "^1.99.0",
    "ts-jest": "^29.4.9",
    "typescript": "^6.0.3"
  }
}
